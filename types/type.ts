export type LoginProps = {
  email: string;
  password: string;
};
export type UserProps = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};

export type ProjectProps = {
  title: string;
  description: string;
  gitLink: string;
  liveLink: string;
  imageUrl: string;
};
